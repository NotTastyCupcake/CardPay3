<reactiveui:ReactiveWindow x:Class="Metcom.CardPay3.WpfApplication.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:reactiveui="http://reactiveui.net"
             xmlns:vms="clr-namespace:Metcom.CardPay3.WpfApplication.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             x:TypeArguments="vms:SettingsViewModel"
             xmlns:local="clr-namespace:Metcom.CardPay3.WpfApplication.Views" 
             mc:Ignorable="d" 
             Title="CardPay - Settings" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="550">

            <TextBlock Text="Тип базы"/>
            <ComboBox x:Name="TypesComboBox" ItemsSource ="{Binding DBTypes}" SelectedItem="{Binding SelectedDataBaseType, Mode=TwoWay}"/>
            <Label Name="TypesError" Foreground="Red"/>

            <TextBlock Text="Строка подключения"/>
            <TextBox x:Name="ConnectionString"/>
            <Label Name="ConnectionStringError" Foreground="Red"/>

        </StackPanel>

        <Button x:Name ="SaveButton" Grid.Row="1" Grid.ColumnSpan="2" Content="Применить"/>

    </Grid>
</reactiveui:ReactiveWindow>
